# CLAUDE.md — Bối cảnh dự án songnhue

## Dự án là gì

Hệ thống quản lý điều hành công trình thủy lợi + Cổng thông tin điện tử cho **Công ty TNHH MTV Đầu tư Phát triển Thủy lợi Sông Nhuệ**.

**Ưu tiên xuyên suốt** (theo thứ tự): độ chính xác → nghiệp vụ chuẩn → tối ưu → vận hành/bảo trì → khả năng scale.

## Cấu trúc tài liệu (đọc theo thứ tự này)

| File | Vai trò |
|---|---|
| `function-spec.md` | **Nguồn sự thật về nghiệp vụ** — đặc tả 5 module, trường dữ liệu, workflow, validation, RBAC, NFR |
| `implement.md` | Kế hoạch implement — gom 4 nhóm (A Core / B Content / C Operations / D HR), thứ tự phase, cấu trúc code, checklist quyết định |
| `architecture-review.md` | Quyết định kiến trúc/tech ĐÃ CHỐT + lý do — khi mâu thuẫn với 2 file trên, file này thắng |
| `conventions.md` | Convention coding/design/security + đặc tả Common Platform (envelope, exception, error code, middleware, utils, RBAC 3 tầng, chống giả mạo) — **luật** bắt buộc khi viết code |
| `docs/coding-guide.md` | **Cách làm** — công thức viết một chức năng nghiệp vụ theo thứ tự (migration → entity → workflow → service → controller → seed quyền → mã lỗi → test), kê đủ những gì Core cho sẵn để không ai dựng lại, kèm các bẫy đã trả giá. Đọc cùng `conventions.md`: file kia là luật, file này là đường đi |
| `docs/ui-styles.md` | **Quy chuẩn UI styles** — triết lý thiết kế, bảng màu (qua `design-tokens`), typography (Noto Sans), spacing, animation (nguyên tắc + danh sách được phép/cấm), component styling, responsive, accessibility. Đọc trước khi sửa CSS/theme/styling ở `admin-app` hoặc `public-web` |
| `business-open-questions.md` | Phần I-A: BOQ đợt 1 **đã đóng**. Phần I-B: **9 mục G đợt 2 đã đóng**. Phần II: **6 mục còn mở** cần khách cung cấp. Phần III: **truy vết chức năng nào còn chứa điểm chưa chốt** — đọc trước khi code 1 chức năng |
| `phase0-tracking.md` | **Bảng theo dõi tiến độ Phase 0** — 11 hạng mục WS-1→WS-11, **107 task** dạng checkbox, mỗi WS tự chứa điều kiện tiên quyết/đầu ra/cách kiểm chứng. Tick khi làm xong; cuối file là **21 mục Definition of Done** |
| `phase1-tracking.md` | **Bảng theo dõi tiến độ Phase 1** (CMS + master data công trình) — 11 hạng mục WS-12→WS-22, **101 task**, 17 mục Definition of Done, sổ nợ riêng. ⭐ Chứa mục **"Nghiệp vụ — 18 điểm đã làm rõ trước khi code"**: những chỗ spec không nói hoặc nói ra hai nghĩa, kèm cột "ai quyết" (nội bộ / phải hỏi Công ty) |
| `report-templates-proposal.md` | Đề xuất format mẫu báo cáo gửi Công ty duyệt (khung 5 khối + danh mục BC/BCNS/BCQT + trường dữ liệu). Layout chi tiết làm sau, khi vào Phase module tương ứng |
| `docs_origin/Trả lời Business Open Questions 12.8.2026.docx.md` | **Câu trả lời chính thức của khách — đợt 1 (12/8/2026)**. Confirm **đợt 2 (mục G)** nhận qua trao đổi trực tiếp cùng ngày, ghi ở `business-open-questions.md` Phần I-B. Cả hai là nguồn của mọi thay đổi scope trong function-spec v2.2 |
| `docs_origin/SRS_QuanTriDieuHanh_TLSN ver 06.8.2026.docx.md` | **SRS v1.0 (23/07/2026)** — đặc tả yêu cầu chính thức của khách (dự thảo lấy ý kiến). function-spec.md v2.0 đã đồng bộ cấu trúc module + traceability theo file này |
| `docs_origin/Tổng quan HT PM...docx.md`, `docs_origin/Đặc tả hệ thống...docx.md` | Tài liệu gốc từ khách hàng — chỉ tham khảo, đã được tổng hợp vào function-spec.md |

## Module

> ⚠ **Cấu trúc module đã tái tổ chức theo SRS v1.0 (2026-08-06)** — xem `function-spec.md` v2.0 §0.4 + bảng traceability §10.

- **MOD-01** Cổng TTĐT/CMS (bài viết workflow duyệt, danh mục, media, banner, liên hệ, khảo sát/góp ý, tìm kiếm, widget thủy văn, **+ liên kết hệ thống văn bản điều hành CN-01.7** — *không đồng bộ dữ liệu, chỉ lưu mã số + auto-login*)
- **MOD-02** Vận hành công trình + GIS (danh mục công trình, **lịch sử sửa chữa/bảo trì/khắc phục sự cố = chức năng ghi nhận chính**, **tình hình vận hành cống CN-02.11 nhập tay + danh mục mã CRUD**, tài liệu, bản đồ GIS nhiều lớp, dashboard điều hành + wall 4K, thống kê, nhật ký thay đổi hồ sơ). ❌ **Đã loại khỏi scope**: nhật ký vận hành · **phiếu sự cố riêng** (gộp vào `maintenance_logs` — chốt G1)
- **MOD-03** Quản lý dữ liệu thủy văn (**tách riêng khỏi MOD-02 cũ**): danh mục điểm đo & loại chỉ số, polling API bên thứ 3 (`songnhue.bhh40.net`), chuẩn hóa/validate (**2 mức Hợp lệ/Nghi ngờ**), time-series, biểu đồ + biểu tổng hợp theo tuyến sông, báo cáo thủy văn, cảnh báo ngưỡng, hiển thị GIS — lõi kỹ thuật
- **MOD-04** HRM (sơ đồ tổ chức, hồ sơ CBNV, nghỉ phép — tuân thủ NĐ 13/2023, BLLĐ 2019) — *trước là MOD-03*
- **MOD-05** Quản trị (RBAC chi tiết, audit, backup/**restore UI**, health-check, thông báo hệ thống, quản lý phiên + đăng xuất từ xa, cảnh báo đăng nhập bất thường, xuất/nhập cấu hình)

## Tech stack (đã chốt — không tự ý đổi)

PostgreSQL 16 + PostGIS · Spring Boot 3 (Java 21) · Next.js (public, SSR/ISR) + React/Vite/AntD 5 (admin) · **Không Redis (v1)** — cache in-process (Caffeine) + bảng `hydro_latest`; denylist ở DB · **DB-backed job queue + ShedLock (giữ sẵn, bật khi ≥2 node)** · **Worker in-process (v1)** · MinIO · ECharts · Leaflet/MapLibre + OSM · Flyway · Auth: access token 30' + refresh rotation httpOnly cookie · **Modular Monolith 1 node (v1), stateless để thêm node = đổi cấu hình** · ArchUnit enforce boundary.

## Quy tắc bất di bất dịch khi code

1. Timestamp lưu `timestamptz` UTC; hiển thị UTC+7. Không lưu giờ địa phương.
2. NUMERIC/BigDecimal cho mọi số đo và tiền — cấm float/double.
3. Mọi giá trị tính toán (tổng hợp kỳ, chi phí bảo trì, số dư phép) tính ở BE; FE chỉ hiển thị.
4. Đổi trạng thái entity chỉ qua Workflow engine (Core) — không UPDATE status trực tiếp. **Trạng thái công trình là giá trị dẫn xuất** (sự cố đang mở → bảo trì → cảnh báo ngưỡng → ánh xạ mã tình hình vận hành → bình thường), không có cột cho người dùng sửa tay.
5. Data scoping theo Xí nghiệp/đơn vị ở tầng repository filter, không dựa vào dev nhớ thêm WHERE.
6. Module không import repository của module khác — chỉ gọi qua service interface.
7. `org_units` là 1 bảng dùng chung cho cả Xí nghiệp (MOD-02) và phòng ban (MOD-04 HRM).
8. Raw data thủy văn (`hydro_raw_logs`) append-only; báo cáo/dashboard đọc từ bảng agg, không scan raw.
9. Soft delete + audit log (old/new value) cho mọi entity nghiệp vụ.
10. Trường nhạy cảm HR (🔒 trong spec): bảng riêng `employee_sensitive`, AES-256-GCM, key ngoài DB.
11. Mọi connection/setup (DB, MinIO, SMTP, API ngoài...) đọc từ env — cấm hardcode; thiếu env bắt buộc → fail-fast lúc startup; client khởi tạo qua Spring bean, không tạo trực tiếp trong code nghiệp vụ.
12. **Tham số nghiệp vụ để trong bảng `settings` có UI sửa** (giờ hành chính 8–17h, retention 5 năm, thông số phép năm, chu kỳ polling, ngưỡng, giới hạn số lượng...) — không nằm trong `application.yml`, không hard-code.
13. **Credential bên thứ 3** (key API thủy văn, mã số hệ thống văn bản của từng user): AES-256-GCM, key ngoài DB, không log, không trả ra API, không nằm trong bản export cấu hình — xem `conventions.md` §4.7.
14. Dữ liệu thủy văn `quality = NGHI_NGO` **vẫn nằm trong bảng chính** → mọi truy vấn báo cáo/alert/tổng hợp **phải lọc `quality = HOP_LE`**. Đây là bẫy sai số liệu dễ mắc nhất.
15. Sự cố **không phải entity riêng** — là `maintenance_logs` với `loại = Khắc phục sự cố` (chốt G1). Không tạo bảng `incidents`, không mã `SC-`.
16. Danh mục do khách vận hành (mã tình hình vận hành, mức ngưỡng, nhóm người nhận cảnh báo) là **dữ liệu có CRUD**, không phải enum trong code — thêm mã mới không được đòi deploy.
17. Poller thủy văn: cron **2 phút/lần vào phút lẻ, giây 45**; **rate-limit trước khi mở HTTP** — bỏ lượt gọi khi *toàn bộ* trạm đã có bản ghi của khung 10' hiện tại (không phải "đã có bản ghi đầu tiên"). Nguồn trả rải rác trong cửa sổ `x1:30 → x8:30`.
18. Không có API lịch sử → **mất dữ liệu là vĩnh viễn**. Ghi nguyên văn response vào `hydro_raw_logs` trước khi parse; giám sát poller ưu tiên ngang backup DB.

## Trạng thái & mục chờ confirm

**📌 Phase "Tài liệu hệ thống" — HOÀN THÀNH ngày 2026-08-12.** BOQ đợt 1 (A–F) + **8/12 mục đợt 2 (G)** đã đóng và đồng bộ vào function-spec **v2.2** / implement / architecture-review §8 / conventions.

- ✅ Confirmed (2026-08-06): tái cấu trúc module theo SRS; Restore qua UI (M5.11) + bảo vệ nhiều lớp.
- ✅ Confirmed đợt 1 (2026-08-12): bỏ nhật ký vận hành → lịch sử sửa chữa · bỏ kế hoạch vụ mùa · bỏ diện tích tưới tiêu · bỏ trạng thái tổ máy realtime · lưu vực = trường text · thủy văn 2 mức chất lượng · bỏ SMS v1 (thông báo qua website + email) · TV 85" 4K · chỉ tiếng Việt · không migrate web cũ · quan hệ điểm đo↔công trình n–n có vai trò · mọi tham số để config.
- ✅ **Confirmed đợt 2 (2026-08-12)**: **G1** gộp sự cố vào lịch sử sửa chữa (PA A) · **G2** không cần giờ chạy máy/kWh/m³ → bỏ vĩnh viễn · **G3** chấp nhận không có API lịch sử; **cron 2'/phút lẻ + rate-limit theo khung 10'**; trạm trục trặc → **GIS xám** · **G4** tình hình vận hành cống nhập tay, **danh mục mã có CRUD + màu + ánh xạ trạng thái** (CN-02.11) · **G7** audit 5 năm rồi kết xuất lưu trữ · **G9** Admin tự cấu hình ngưỡng (màn hình cấu hình là hạng mục nghiệm thu) · **G11** người nhận = nhóm "Ban điều hành" ∪ người phụ trách công trình · **G12** chốt số NFR nghiệm thu (99% · 200 CCU · 3s · 60s · 2FA Admin).
- ✅ **API thủy văn đã đấu nối được (12/8/2026)**: `GET http://songnhue.bhh40.net/api/getmn.aspx?key=<mã số>;` — **dấu `;` cuối key bắt buộc**, thiếu thì trả `not.working`. Response text, phân tách `<br>`, bản ghi `F#####;dd/MM/yyyy;HH:mm;value=<cm>;` — 19 điểm mực nước, đơn vị **cm** (chia 100 ra m). Đặc tả parser: `function-spec.md` CN-03.2.
- ⛔ **Giới hạn nguồn**: **không có API lịch sử** (đã chấp nhận — poller là nơi bắt dữ liệu duy nhất, mất là mất vĩnh viễn, giám sát như backup) · **không có API lượng mưa** (v1 hiển thị `-`) · **không trả tên điểm đo, chỉ trả mã**.
- ✅ **G8b ĐÃ ĐÓNG (12/8/2026)** — Công ty cấp đủ **19/19 mã API ↔ tên điểm đo + vai trò**; bảng seed ở `function-spec.md` CN-03.1. **Không còn mục nào chặn.** 3 hệ quả: thêm vai trò **`MN_SONG`** (điểm loại này có thể không gắn công trình nào) · **cấm validate "TL > HL"** (2/5 cặp đảo hợp lệ) · seed/join **dùng mã, cấm dùng tên** (có 2 công trình cùng tên "Yên Nghĩa").
- ⬜ **Còn mở 6 mục, chỉ ảnh hưởng dữ liệu khởi tạo & nghiệm thu**: **G8** tuyến sông/lý trình/tọa độ + khoảng trống API-vs-biểu tổng hợp + 3 cặp mã trùng giá trị + danh mục công trình · **G3-a** lượng mưa · **G5** mã số hệ thống văn bản (+ xin SSO) · **G6** mẫu 2C-BNV · **G9-a** bộ mức ngưỡng · **G10** duyệt format báo cáo.
- ✅ **Đã verify sẵn sàng code (2026-08-13)**: Phase 0/1/2 **bắt đầu được ngay**; chỉ **CN-01.7 (lưu mã số) bị chặn bởi G5** → tách task riêng. Môi trường máy dev đủ (JDK 21 · Node 22 · Docker+Compose · psql 17); chưa cài Maven/Gradle nhưng dùng wrapper là được. Repo chưa có dòng code nào → greenfield. Chi tiết: `implement.md` §7.
- 📋 **Bảng truy vết "chức năng nào còn chứa điểm chưa chốt"**: `business-open-questions.md` **Phần III** — dev đọc trước khi bắt tay vào 1 chức năng.
- ✅ **Phase 0 đã có kế hoạch chi tiết (2026-08-13)** — `phase0-tracking.md`: 11 hạng mục, ~113 task, ~114 người-ngày. Quyết định nền tảng ghi ở `architecture-review.md` **§9**: Maven multi-module · monorepo · docker-compose **3 VM** · secrets env + GitHub Secrets · **migration chạy ở service `migrator` riêng** · **DB roles tách quyền**.
- ⚠ **Backup đã hạ xuống bản tối giản (13/8/2026)**: `pg_dump` hàng đêm, **RPO ≤ 24h · RTO ≤ 4h**, **không PITR/WAL/replica** — chấp nhận mất tối đa 1 ngày dữ liệu. Bảng 4 rủi ro chấp nhận ở `architecture-review.md` §6.5. Đây là quyết định nội bộ, **không hỏi khách**.
- ✅ **WS-1 xong (13/8)**: monorepo + Maven multi-module 6 module + Spotless/Checkstyle/ESLint/Prettier + hook commit-msg + Makefile.
- ✅ **WS-2 xong (14/8)**: 9 migration Flyway đa module · 25 bảng Core + 15 partition audit · 4 DB role tách quyền · 88 permission / 12 vai trò / 334 dòng phân quyền dịch từ ma trận §6 · 55 tham số `settings`. **Hash chain audit tính bằng trigger trong DB** (app không giả được chuỗi); `audit_logs` partition theo tháng. Tài khoản `superadmin` seed **không có mật khẩu** — kích hoạt bằng lệnh bootstrap ở T5.7.
- ✅ **WS-3 xong 6/7 (14/8)**: 4 chế độ chạy local chọn bằng Compose profile (`dev-infra` / `dev-be` / `dev-fe` / `dev-docker`) — image build từ mã nguồn local ngay trong container, build lại ~7 giây. Cổng Docker tách hẳn dải riêng + bind `127.0.0.1`. Collation DB chốt **ICU `vi-VN`**. Tài liệu: `docs/setup-guideline.md`, `docs/run-guideline.md`. Còn T3.4 (2 image FE) chờ WS-8/WS-9.
- ✅ **WS-4 xong (14/8)**: Common Platform ở `core.common.*` — envelope + `traceId` phủ 100% endpoint · **31 mã lỗi** · 8 subclass exception, lỗi lạ → `SYS-0001` không lộ chi tiết · rate limit 3 nhóm · 8 utils dùng chung · `BaseEntity`/`ScopedEntity` · fail-fast cấu hình · OpenAPI 6 nhóm. **`core.common.*` là ngoại lệ được import chéo** — ArchUnit (T10.2) phải theo đúng đó.
- ✅ **WS-5 xong (14/8)** — nơi dồn công của Phase 0: JWT **RS256** có `kid` · refresh rotation + **reuse detection** (thu hồi cả family, access token chết ngay nhờ claim `fid`) · CSRF double-submit · lockout không tiết lộ user tồn tại · **2FA TOTP** tự cài theo RFC 6238 (khớp 6/6 vector chuẩn) · **RBAC 3 tầng** với 3 annotation bắt buộc khai báo tường minh · quản lý phiên + đăng xuất từ xa · lệnh bootstrap `superadmin`. **36 mã lỗi**, 129 test xanh. **Không dùng filter chain Spring Security** — lý do ghi ở `architecture-review.md` **§9.5**.
  - ⚠ **Rate limit đăng nhập nâng 5→30 lượt/15' theo IP**: để bằng 5 thì khoá tài khoản (`AUTH-0003`) không bao giờ kích hoạt được và tham số M5.15 vô nghĩa; thêm nữa cả Công ty ra Internet qua một IP NAT nên hạn mức quá chặt là cả cơ quan không đăng nhập được. Có test chặn ở CI.
  - ⚠ **`@EntityScan` + `@EnableJpaRepositories` phải khai tường minh** ở `SongnhueApplication` — `scanBasePackages` không áp cho JPA.
- ✅ **Rà soát nợ tồn WS-1→WS-5 (14/8)** trước khi mở WS-6. Lập **"Sổ nợ liên WS"** ở `phase0-tracking.md` (18 dòng) — trước đó nợ chỉ ghi ở WS *giao*, **WS nhận không có task nào đứng tên** (8 mục). Kèm **luật 3 bước khi đóng WS**: tick task → tick dòng nợ → **quay lại sửa mô tả đã lỗi thời ở WS đã giao nợ** (bước 3 hay bị bỏ nhất).
  - ⚠ **Lỗi thật phát hiện khi rà soát: T4.8 đã tick nhưng fail-fast KHÔNG hoạt động.** `@Validated` + `@NotBlank` không bắt được biến môi trường bị thiếu — `@ConfigurationProperties` bỏ qua placeholder không giải được nên trường nhận nguyên văn `"${MINIO_ENDPOINT}"`, không rỗng, validate đi qua. Chạy thật: bỏ hẳn `MINIO_ENDPOINT` → app `Started`, health `UP`. Đã thêm **`UnresolvedPlaceholderGuard`** quét mọi bean `@ConfigurationProperties` → **đóng DoD mục 3**. **138 test xanh.**
  - ⚠ **`conventions.md` §4.5 từng tự mâu thuẫn với §4.1** (5/15' vs 30/15' cho rate limit login) — đã thống nhất về **30/15'**.
- ✅ **WS-6 xong (15/8)** — khối lớn nhất Phase 0: 6 pattern P1–P6 thành shared service (cây tổ chức · tệp đính kèm MinIO · workflow engine · thông báo in-app+email · hàng đợi job SKIP LOCKED · settings có UI) + nhật ký kiểm toán tự động + kết xuất audit 5 năm + lát cắt dọc CRUD tài khoản/vai trò. **43 mã lỗi**, **184 test xanh**. Trả **6 dòng nợ** (#9–#14).
  - ⚠ **ShedLock KHÔNG bọc quanh worker hàng đợi** — hai bài toán ngược nhau (`architecture-review.md` **§9.6**). Job theo lịch chỉ *đặt việc* vào hàng đợi, khoá chống trùng theo ngày làm DB thành điểm đồng bộ, nên cũng không cần ShedLock cho nhóm đó.
  - ⚠ **Ba bẫy auto-config của Spring Boot đã sập, cả ba đều im lặng** (`architecture-review.md` **§9.7**): khai bean `DataSource` hoặc `JdbcTemplate` làm Boot ngừng tạo bản chính (cả app chạy bằng vai trò `songnhue_archiver`); `@ConditionalOnBean` trên `@Component` không đáng tin.
  - ⚠ **Đăng ký Hibernate listener sau khi app đã lên KHÔNG có tác dụng** — Boot 6 chốt nhóm listener lúc dựng SessionFactory. Phải cắm bằng `Integrator`. Triệu chứng: `audit_logs` trống rỗng mà không lỗi nào.
  - ⚠ **`@Modifying` hàng loạt không đi qua bộ ghi nhật ký** — thao tác cần dấu vết phải đi qua entity hoặc gọi `AuditService.record(...)`.
- ✅ **WS-10 xong (15/8)** — làm trước WS-7 để rà soát chất lượng những gì đã dựng. 14 luật ArchUnit · ma trận RBAC đối chiếu 334 dòng phân quyền trong DB · chuỗi hash audit trên DB thật · cổng bao phủ JaCoCo tầng domain · `ci.yml` 3 job. **226 test xanh** (181 core + 45 app). Trả 4 dòng nợ (#5–#8).
  - ⚠⚠ **Phát hiện nặng nhất Phase 0: tầng 3 phân quyền chưa từng hoạt động.** `ScopeFilterAspect` đặt `@Order(LOWEST_PRECEDENCE - 1)` với ý định "nằm trong bộ chặn transaction", nhưng trong Spring AOP **số nhỏ hơn là vòng NGOÀI** — `enableFilter` rơi vào một `Session` tạm bị vứt đi, truy vấn thật chạy không có lọc. **Mọi Xí nghiệp đọc được dữ liệu của nhau, không một dòng lỗi nào.** Sửa bằng cách kéo bộ chặn transaction lên `CorePlatformConfig.TRANSACTION_ADVISOR_ORDER` (`architecture-review.md` §9.8.1).
  - ⚠ **Bốn cơ chế canh gác "xanh mà không chạy"**: bộ máy JUnit của ArchUnit tìm ra 0 bài kiểm (đặt luật chắc chắn sai vẫn xanh) · luật JaCoCo bị bỏ qua vì `<includes>` đặt trong `<rule>` so với *tên phần tử* · 2 luật chạy qua 0 lớp. **Chốt cách làm: mỗi cơ chế canh gác phải có bài kiểm chứng minh nó bắt được vi phạm** (`conventions.md` §1.5).
  - Thêm **`ScopeGuard`**: bản ghi ngoài phạm vi trả `AUTH-3002` + ghi `security_events` thay vì 404 im lặng — trước đó `AUTH-3002` là mã lỗi chết, có trong tiêu chí nghiệm thu mà không dòng mã nào ném ra.
  - Bộ luật T10.2 bắt được **3 vi phạm phân tầng có thật** ngay lần chạy đầu (controller gọi thẳng repository · record của `infra` lọt ra `api` · `WorkflowAware` sai tầng).
- ✅ **CI/CD 3 chặng + bảo vệ nhánh đã áp dụng (15/8)**: `dev` → `staging` → `production`, `master` ngoài luồng. **Kiểm một lần ở `dev`, đóng gói một lần** — image gắn tag theo commit SHA, hai chặng sau đề bạt chính image đó, không build lại. 4 workflow (`ci` · `promotion-guard` · `deploy-staging` · `deploy-prod`), tài liệu `docs/cicd.md` + `docs/branch-protection.md`. CI lọc theo **vùng đường dẫn** (BE/FE/tài liệu) nhưng **cố ý không lọc theo module backend** — 4 lỗi nặng nhất WS-10 đều sửa ở `core` mà bị bắt bởi bài kiểm ở `app`.
  - ⚠ **Kiểm chứng ngược bằng API tìm ra 3 lỗi trong chính tài liệu** (nợ #27, lệnh sửa ở `docs/branch-protection.md` §6.2): **`strict: true` ở staging/production tự khoá chặng đề bạt sau lần merge đầu** (dev không thể "Update branch" vì cả merge lẫn rebase đều vi phạm bảo vệ của chính nó) · **job lọc `Vùng nào thay đổi` không nằm trong `contexts`** nên nó hỏng thì 2 job nặng bị skip mà **skip được tính là đạt** · **đội 1 người mà đòi 1 lượt duyệt là cấm merge** (GitHub cấm tự duyệt PR) nên mọi lần merge phải bấm bypass, mà bypass bỏ qua luôn status check.
  - ⚠ **Hai quy ước merge ngược nhau**: feature → `dev` phải **Squash/Rebase** (linear history); `dev` → `staging` → `production` phải **merge commit** (deploy tìm image qua `HEAD^2`).
  - ⚠ **Mã chưa lên `dev`** — đo lại 17/8: `common` đi trước `dev` **22 commit / 431 tệp**; `dev` có 12 tệp tài liệu nhưng **không có mã nguồn và không có `.github/`**, nên repo **vẫn 0 lượt CI**. Gỡ bằng PR `common → dev` (nợ #28).
- ✅ **WS-7 xong 11/12 (16/8)** — sao lưu, khôi phục và giám sát. `pg_dump -Fc` hằng đêm 02:00 + theo yêu cầu (M5.10), sổ đăng ký `system_backups` **ghi cả lượt hỏng** · khôi phục qua UI (M5.11) **6 lớp chặn** · maintenance mode chặn ghi 503 · 4 health indicator + `GET /api/v1/system/health` (M5.12) · 3 gauge + counter sự kiện bảo mật · Prometheus/Grafana trên VM-3 + **14 luật cảnh báo** · 4 script vận hành · **7 runbook** ở `docs/runbook/`. **49 mã lỗi**, **255 test xanh**. Trả nợ #18, #21. Còn T7.7 (diễn tập khôi phục đo RTO thật) chờ VM-2.
  - ⚠ **`@Transactional` trên phương thức TỰ GỌI trong cùng lớp không có tác dụng** — self-invocation không đi qua proxy Spring. Bản đầu `BackupService` dính đúng bẫy này: dòng `RUNNING` không được commit trước khi `pg_dump` chạy, tức là mất đúng thứ cơ chế đó sinh ra để giữ (dấu vết khi tiến trình chết giữa chừng). Dùng `TransactionTemplate` như `JobService`/`SecurityEventService`.
  - ⚠ **`CHAR(n)` trong migration vs `String` trong entity** → `ddl-auto: validate` chặn **toàn bộ** context test tích hợp: 18 bài đỏ vì một cột. Luôn dùng `VARCHAR`.
  - ⚠ **Đọc luồng đầu ra tiến trình con tới EOF trước `waitFor(timeout)` làm hạn chờ vô hiệu** — tiến trình treo mà không ghi gì thì kẹt vĩnh viễn ở `readLine()`. Phải đọc ở luồng riêng.
  - **Kho sao lưu KÉO về VM-3, không đẩy đi từ VM-1** (`architecture-review.md` §9.9.1): VM-1 không giữ khoá ghi vào kho, nên chiếm được VM-1 vẫn không xoá được bản sao lưu. `prod.env.example` đã **bỏ** `BACKUP_TARGET_HOST` của bản WS-3.
  - **`pg_dump` chạy bằng `songnhue_readonly`; khôi phục là tính năng BẬT RIÊNG** — `DB_RESTORE_PASSWORD` để trống là lựa chọn hợp lệ, UI báo `ADM-2010` và khôi phục đi bằng runbook.
- ✅ **WS-8 xong (17/8)** — admin-app: Vite 8 + React 18 + TS strict + AntD 5 + TanStack Query + React Router 7. `apiClient` là HTTP client **duy nhất** (ESLint chặn `axios`/`fetch` chỗ khác) · **8 màn hình quản trị + 2 màn hình cá nhân** phủ MOD-05 · 7 component nghiệp vụ dùng chung · `error-map.ts` mirror **49 mã**. **24 test xanh**, image Docker build + chạy thật. Trả nợ #4, #32, #34 và **nửa** #15. Quyết định ghi ở `architecture-review.md` **§9.10**.
  - ⚠ **Access token chỉ nằm trong bộ nhớ**, refresh token trong cookie `httpOnly` — một lỗ XSS chỉ lấy được vé sống 30 phút. Cái giá phải nhận: F5 mất token, nên `bootstrapSession()` gọi `/auth/refresh` một lần lúc khởi động. Trạng thái xác thực có **ba** giá trị (`loading`/`anonymous`/`authenticated`), thiếu `loading` là route guard chuyển hướng ngay lúc chưa biết là ai.
  - ⚠ **Làm mới token phải đúng một lượt** — refresh **xoay vòng** token, gọi song song là tự kích hoạt cơ chế phát hiện dùng lại của backend, thu hồi cả family.
  - ⚠ **`tsc -b --noEmit false` đẻ 49 tệp `.js` ngay trong `src/`** — `--noEmit false` ghi đè tsconfig, không có `outDir` nên ghi cạnh mã nguồn. Lint, typecheck, build đều xanh; chỉ `prettier --check` bắt được một tệp lọt ra ngoài `src/`.
  - ⚠ **ESLint 9 flat config KHÔNG gộp cấu hình lồng nhau** — `eslint.config.mjs` đặt trong từng app bị bỏ qua **im lặng**, nhóm rule React không chạy mà lint vẫn xanh. Một file duy nhất ở `frontend/`, khoanh vùng theo `files`.
  - ⚠ **`npm ci` phải chạy ở `frontend/`**, không phải trong từng app: npm workspaces chỉ có **một** lockfile. `ci.yml` viết từ WS-1 sai đúng chỗ này ở cả hai job FE — đã sửa, nhưng chưa chạy thật vì repo còn 0 lượt CI.
- ✅ **WS-9 xong + T3.4 đóng (17/8)** — public-web: Next 16 + Tailwind 4 + TS strict · layout công khai · SEO base (`sitemap.ts`, `robots.ts` **tự chặn lập chỉ mục ở staging/local** vì cùng mã nguồn) · ISR + `POST /api/revalidate` cho luồng duyệt bài Phase 1 · `GET /api/health` · `output: standalone`. Tách **workspace thứ ba `frontend/design-tokens`** để hai app FE ngang hàng cùng phụ thuộc. **`make dev-docker` → 6 container healthy, admin-app + public-web + API trả lời cùng lúc** → đóng **DoD mục 2** và nợ #15. **259 test BE** (209 core + 50 app) + **24 test FE**. Quyết định ghi ở `architecture-review.md` **§9.11**.
  - ⚠⚠ **Phát hiện nặng nhất của WS-9 không nằm trong WS-9: image backend đang chạy là bản dựng WS-3, không có controller nào.** `make dev-*` chỉ build lại khi gõ `BUILD=1`, nên suốt WS-4→WS-8 container `healthy`, `/actuator/health` xanh, mà **mọi endpoint `/api/v1/**` trả 404**. Đổi mặc định thành **luôn build lại** (`NOBUILD=1` để bỏ qua) — đo thật ~10 giây khi mã nguồn không đổi. Bài học lặp lại lần thứ ba: **healthcheck trỏ vào endpoint không đại diện cho thứ cần kiểm thì nó chỉ chứng minh tiến trình còn sống**.
  - ⚠ **Migrator không bao giờ thoát** — migration chạy xong, in "✓ Migration hoàn tất", rồi treo vĩnh viễn vì `@EnableScheduling` dựng luồng **không phải daemon** và worker hàng đợi chạy vòng lặp; `app` kẹt ở `Created`, không một dòng lỗi. Đây đúng là cơ chế `depends_on: service_completed_successfully` mà T11.4 dựa vào. Sửa bằng `SchedulingConfig` mang `@Profile("!migrate")` + `worker-enabled: false` + `lazy-initialization: true`; canh bằng `MigrateProfileTest` — có bài **cấm `@EnableScheduling` xuất hiện ở lớp khác**, vì đó mới là đường quay lại của lỗi.
  - ⚠ **`spring.main.web-application-type: none` KHÔNG ngăn Spring dựng controller** — nó chỉ tắt cổng HTTP. Hệ quả: migrator kéo theo `AuthController → AuthService → TokenService → JwtKeyStore` và **đòi khoá ký JWT** dù việc duy nhất của nó là chạy DDL.
- ✅ **Rà soát nợ + kiểm chứng lại các WS đã đóng (17/8)** — không thêm chức năng, chỉ chạy tay lại những thứ đã tick. **4 lỗi thật, cả 4 im lặng** (`architecture-review.md` §9.12); **263 test BE** (thêm 4 bài canh gác) + 24 test FE.
  - ⚠⚠ **Sao lưu chưa từng sinh ra một tệp nào kể từ WS-7** — `pg_dump` chết vì `permission denied for sequence system_backups_id_seq`. `ALTER DEFAULT PRIVILEGES` cho vai trò dump chỉ khai `TABLES`, **thiếu `SEQUENCES`**, nên bảng đầu tiên tạo sau nó — **chính bảng sổ đăng ký sao lưu** — chặn mất cơ chế sao lưu. Đây là lưới an toàn *duy nhất* của hệ (không PITR, không replica). 255 bài kiểm không bắt được vì **`BackupServiceTest` mock `PostgresToolRunner`** → **mock đặt đúng chỗ mã chạm ra ngoài là chưa kiểm gì cả** (`conventions.md` §1.5).
  - ⚠⚠ **`verify-no-keys.sh` báo ĐẠT mà chưa từng quét khoá PEM** — mẫu bắt đầu bằng `-----BEGIN` bị `grep` đọc thành tuỳ chọn, grep chết, `if` nuốt lỗi, script in `✓`. Nay có **tự kiểm mỗi lượt**; kiểm chứng ngược bằng khoá giả cắm vào CSDL.
  - ⚠ **`make migrate` chạy image cũ** — WS-9 đổi mặc định luôn build cho `dev-*` nhưng **bỏ sót `migrate`**; migration mới không chạy mà vẫn in "✓ Migration hoàn tất". Nguy hiểm hơn `dev-*` vì migration nằm trong jar.
  - ⚠ **CSDL test có schema production không có** (`topology`, `tiger`) — `withCopyFileToContainer` chép *vào* thư mục chứ không đè, nên `10_postgis.sh` của image vẫn chạy; ở production bind-mount của compose che mất. Lệch theo cả hai chiều: bài kiểm đỏ vì thứ không có thật, và mã dùng hàm `topology` sẽ xanh ở đây rồi hỏng khi chạy thật.
  - ⚠ **Smoke test 2 workflow deploy hỏi `/actuator/health`** (bản tổng) — bản tổng cố ý DOWN khi chưa từng sao lưu, nên **đúng lần deploy đầu tiên** smoke test đỏ 5 phút rồi coi như hỏng. Đổi sang `readiness`; **chưa chạy thật** (nợ #41).
- ✅ **Chốt vòng đời mật khẩu + luồng quên mật khẩu (18/8) — đóng nợ #35**, phần dựng thuộc Phase 1. Đặc tả `function-spec.md` **M5.15-a**, lý do `architecture-review.md` **§9.13**: hạn **90 ngày** → khoá tài khoản · tự đặt lại **giãn cách 90 ngày**, trong kỳ thì quản trị viên cấp mật khẩu tạm · **đổi chủ động không giãn cách** · nhắc email 14/7/1 ngày trước hạn.
  - ⛔ **Email tự phục vụ gửi LIÊN KẾT một lần (TTL 30'), không gửi mật khẩu** — gửi mật khẩu mới theo yêu cầu chưa xác thực thì ai biết tên đăng nhập cũng vô hiệu hoá được mật khẩu người khác. Mật khẩu tạm chỉ có ở đường quản trị viên cấp.
  - ⚠ Quy tắc khoá áp cho **cả Super Admin** (miễn trừ là làm rỗng chính sách) → đường thoát là **lệnh chạy trên máy chủ**, mở rộng `AdminBootstrapRunner`, không phải ngoại lệ trong mã.
- ✅ **Lượt CI đầu tiên của repo chạy thật (18/8)** — PR #1 `common → dev`, trả nợ #24 + #36: backend xanh 2'09", frontend xanh 57s. ⚠ **Job quét CVE timeout 30' rồi bị huỷ** (thiếu `NVD_API_KEY` → NVD giới hạn tốc độ, phải tải 378.798 bản ghi). Hai bài học: **`continue-on-error` chỉ áp cho job *thất bại*, job bị huỷ vì timeout thoát khỏi nó** và nhuộm `cancelled` lên cả lượt chạy · **nhịp quét CVE không phải nhịp của PR** — tách sang `security-scan.yml` chạy hằng đêm, cache CSDL NVD riêng, thiếu khoá thì bỏ qua và nói to; PR giữ `dependency-review-action` (vài giây). Nợ #42 — **cần đặt secret `NVD_API_KEY`**.
  - ⚠⚠ **Lượt CI thứ hai lộ ra lỗi nặng hơn: bộ lọc đường dẫn bỏ qua job backend NGẪU NHIÊN.** `echo "$changed" | grep -q` dưới `set -o pipefail` — grep khớp rồi thoát sớm, `echo` nhận SIGPIPE (141), pipeline coi như thất bại **dù đã khớp**, nhánh `else` đặt `backend=false`. Cùng một PR: lượt 12:56 backend chạy, lượt 13:34 bị bỏ qua; dấu vết duy nhất là dòng `echo: write error: Broken pipe`. Nguy hiểm vì **`skipped` của một required check được GitHub tính là ĐẠT**. Vá bằng here-string + **mặc định `true`** (bộ lọc trục trặc thì chạy thừa, không bỏ sót).
  - ⚠ **Secret đặt đúng tên là chưa đủ, phải đúng cấp**: `NVD_API_KEY` lần đầu đặt ở **environment `staging`**, mà `security-scan.yml` không khai `environment:` nên giải ra chuỗi rỗng, không lỗi nào. Khoá công cụ CI đặt **cấp repo**; chỉ khoá gắn với môi trường triển khai mới đặt ở environment.
- ✅ **PR #1 merge, mã Phase 0 lên `dev` (18/8)** — trả nợ #28 + #44. `dev` = `f5c5ac4` (Squash), `git diff origin/dev origin/common` **rỗng**. Lượt push sau merge chạy trọn **5/5 job**, gồm **`Đóng gói image` đẩy được lên GHCR** dù repo đặt `default_workflow_permissions: read` (`permissions: packages: write` khai ở job ghi đè được). **DoD mục 19 đóng.**
  - ⚠ **Sửa một nhận định sai của chính tài liệu**: `docs/branch-protection.md` bản 15/8 đếm ra một collaborator rồi kết luận phải hạ số người duyệt xuống 0. Repo có **hai** tài khoản admin, PR #1 merge bình thường với `reviews: 1`, không bypass. Nợ #27 rút 3 mục → **2**. Luật thay thế: **số lượt duyệt bắt buộc không được vượt số người duyệt được** — vượt thì đường merge duy nhất là bypass, mà bypass bỏ qua cả status check. Nói thẳng để không tự huyễn hoặc: hai tài khoản là **cùng một người**, nên luật duyệt đang là *thủ tục*, không phải cặp mắt thứ hai.
- ✅ **Quét OWASP chạy thật lần đầu, và đã xử lý xong (18/8)** — `npm audit` xanh; OWASP **đỏ đúng thiết kế**: **50 CVE ≥ 7** trên 8 artifact, riêng `tomcat-embed-core 10.1.42` có 24 mã (CVE-2026-43512/41293 **9.8**, CVE-2025-55754 9.6). Nguyên nhân gốc: **phụ thuộc chậm 13 bản vá** — `spring-boot 3.5.3` (20/6/2025) trong khi dòng 3.5.x đã tới **3.5.16**. Nâng **một dòng parent** kéo theo cả BOM: spring-framework 6.2.8→**6.2.19**, tomcat 10.1.42→**10.1.55**, spring-security 6.5.1→**6.5.11**, jackson 2.19.1→2.21.4; springdoc 2.8.9→**2.8.17**. **263 test xanh** sau nâng cấp. Còn đúng **1 mã** phải suppress (nợ #46).
  - ⚠⚠ **Bài học nặng nhất: tôi suýt kết luận "không có đường nâng cấp".** API tìm kiếm `search.maven.org/solrsearch` trả về 3.5.3 là mới nhất — **kết quả cũ**. Nguồn đúng là `maven-metadata.xml` ở `repo1.maven.org`. Suýt nữa đã lập suppression cho 49 CVE **có bản vá sẵn**. ⛔ **Luật: nâng cấp trước, suppress sau; và tra phiên bản bằng `maven-metadata.xml`, không bằng API tìm kiếm.**
  - ✅ **Đo thật qua 4 vòng (18/8): 66 → 9 → 3 → 0 CVE ≥ 7 — đóng nợ #48.** ⚠ Nhưng vòng 4 **vẫn đỏ, vì lý do khác hẳn**: `Sonatype OSS Index — 401 Unauthorized` nâng thành `AnalysisException`. Truy ngược cả 4 lượt: analyzer đó **chưa từng chạy được lần nào** (130 cảnh báo mỗi lượt = hỏng ở gần như mọi artifact), ba vòng đầu hỏng ở dạng *cảnh báo* nên trôi qua, tới khi Sonatype chặn ẩn danh thì nó giết build **đúng lúc mọi thứ khác vừa sạch**. Tắt đích danh `ossindexAnalyzerEnabled=false` (nợ #49); ⛔ **không** dùng `failOnError=false` — nó nuốt mọi lỗi phân tích, kể cả của analyzer đang chạy thật. **Bộ nhớ đệm NVD: 24'40" → 19 giây.**
  - **Chi tiết 3 vòng đầu: 66 → 9 → 3.** Vòng 2 (Boot 3.5.16) còn 9 mã, đều ở `tomcat-embed-core 10.1.55` do BOM ghim → đè `<tomcat.version>10.1.57</tomcat.version>` vá hết (gồm 4 mã **9.1** bypass ràng buộc bảo mật ở rewrite valve), 1 mã vào suppression. Vòng 3 còn 3 mã: `log4j-api` (CVE-2026-34479, v3 **7.5**) và `swagger-ui 5.32.2` gói DOMPurify 3.3.2 (CVE-2026-65898, v3 **7.2**) → `log4j2.version` **2.25.5** + ghim riêng `swagger-ui` **5.32.13** (DOMPurify 3.4.13, đã giải nén webjar đọc `DOMPurify.version` để kiểm, không suy đoán). **Bộ nhớ đệm NVD đã lưu được** từ vòng 2 → bản vá `if: always()` hoạt động.
  - ⚠⚠ **Điểm in trong thông báo lỗi KHÔNG phải điểm dùng để chặn.** DC in **CVSS v4**, chặn theo **điểm cao nhất mọi thang** — nên `CVE-2026-34479(6.9)` nằm dưới tiêu đề "≥ 7.0" là đúng, nó có v3 = 7.5. Script của tôi để v4 ghi đè v3 nên **đếm thiếu**, báo nhầm "56 → 6 → 0" (tưởng đã sạch) thay vì "66 → 9 → 3". Mọi script đọc `dependency-check-report.json` phải lấy `max` của `cvssv2`/`cvssv3`/`cvssv4`; trường `severity` cấp trên cùng cũng theo v4 nên **không dùng để lọc được**.
  - **Suppression phải có `until`** — `backend/dependency-check-suppressions.xml`, mỗi mục kèm ngày hết hạn + lý do *không áp dụng* (không phải "chưa có bản vá"). Hai mục, cả hai đều là **lỗ hổng ở thứ ta không phát hành**: CVE-2026-53914 ở `kotlin-stdlib` (lỗi nằm ở *build cache metadata* của bộ công cụ Kotlin — dự án Java thuần dựng bằng Maven) · CVE-2026-66299 ở Tomcat (lỗi nằm ở **ứng dụng ví dụ WebSocket chat**, mà Boot chỉ nhúng `tomcat-embed-core`, không kèm `webapps/examples`; advisory tự nói ai gỡ examples thì không dính).
  - ⚠ **Đã thử `minio 8.6.0` rồi TRẢ VỀ 8.5.17**: okhttp 5.x phát hành kiểu Kotlin Multiplatform, artifact `okhttp` chỉ còn pom trỏ sang `okhttp-jvm` nên Maven không giải được biến thể → biên dịch chết ở `StorageConfig` (`cannot access okhttp3.HttpUrl`) dù mã ta **không import okhttp** — `MinioClient.Builder.endpoint()` có nạp chồng nhận `HttpUrl` nên javac vẫn cần lớp đó.
  - ⚠⚠ **Bộ nhớ đệm CSDL NVD chỉ lưu khi job xanh, mà job này sinh ra để đỏ.** `actions/cache` khai `post-if: success()` → CSDL dựng 26 phút rồi **không được lưu**, lượt sau lại trả đủ 26 phút. Cơ chế tăng tốc chỉ chạy trong đúng trường hợp duy nhất nó không cần thiết. Sửa bằng `cache/restore` + `cache/save` **`if: always()`** + tách `update-only` khỏi `verify` (`docs/cicd.md` §3.3-a).
  - ⚠ **DC bỏ sót CVE im lặng** — lỗi H2 `Value too long for column "URL"` làm 2 CVE không vào CSDL mà build vẫn đi tiếp. Lên `12.1.3`, chưa biết hết chưa (nợ #47).
- ⚠⚠ **Bẫy squash merge sập 2 lần trong ngày 18/8 — nay có cơ chế canh.** Squash tạo commit **mới** mang nội dung nhưng **không mang lịch sử**, nên `dev` đã chứa công việc mà git không biết; tổ tiên chung đứng nguyên và PR sau dựng lại toàn bộ khác biệt. Lần 1: PR hiện **437 tệp** trong khi nhánh chỉ khác **8**. Lần 2: commit chồng lên nền chưa reset → **xung đột thật** ở 3 tệp dù nội dung hai bên **giống hệt nhau**. **Luật: squash xong thì nhánh nguồn đã chết — cắt nhánh mới từ `dev`.** Thi hành bằng `.githooks/pre-push` (`make hooks`), phát hiện bằng cách đếm tệp *hiện trong diff ba chấm mà nội dung đã giống base*. Chi tiết `docs/cicd.md` §9.1.
  - ⚠ Bản đầu của chính phép canh đó **báo đạt mà không bắt được gì** (dựng repo mô phỏng trong subshell rồi kiểm ở ngoài → soi nhầm repo thật). Phép **tự kiểm** bắt được — lần thứ ba trong dự án mà `conventions.md` §1.5 chứng minh giá trị.
- ⚠⚠ **Vì sao OWASP cứ sửa xong lại đỏ (19/8): bốn lượt quét chỉ soi được MỘT module.** Goal `check` chạy riêng từng module, Maven dừng reactor ở module đầu tiên hỏng → `Content/Operations/Hydro/HR/App` in **SKIPPED** suốt vòng 1–4. Mỗi lần dọn sạch `core` thì reactor đi thêm một bước và lộ ra module kế — cảm giác đập chuột chũi, thực chất **chưa bao giờ nhìn thấy toàn cảnh**. Vòng 5 lộ `app` dính **CVE-2026-54291 (8.2)**: pgjdbc 42.7.11 âm thầm hạ cấp `channelBinding=require` → mất lớp chống người-đứng-giữa; nâng **42.7.13**. **Điều đáng sợ hơn: nếu module đầu tình cờ sạch thì ta tưởng cả dự án sạch.** Sửa bằng `dependency-check:aggregate` (gốc + mọi module con trong 1 lượt, `inherited=false`). Luật ở `conventions.md` §4.5 mục 5: **phép kiểm chạy lâu phải ưu tiên báo cáo trọn vẹn hơn là dừng sớm**. Job OWASP làm gì — `docs/cicd.md` §3.3-0, kèm bảng phân biệt **3 kiểu đỏ** (có lỗ hổng thật · hạ tầng quét hỏng · chưa có CSDL NVD).
- ✅ **Rà soát toàn bộ CI/CD + codebase trước khi rời Phase 0 (19/8).** Lượt quét bảo mật **xanh hoàn toàn**: 109 artifact trong một báo cáo · **0 CVE ≥ 7** · 2 mục suppression đang ăn thật · CSDL NVD dựng **31 giây**. Con số codebase khớp thực tế: **88 quyền · 12 vai trò · 49 mã lỗi (BE=FE) · 23 bài ArchUnit · 11 migration · 58 tham số `settings` · 8 runbook**; 334 dòng phân quyền do `RbacMatrixTest` canh trên DB thật. **263 test BE + 24 FE.**
  - ⚠⚠ **4 lỗi thật, cả 4 nằm ở workflow CHƯA TỪNG CHẠY** (nợ #51–#53): hai deploy **tra image trước khi đăng nhập GHCR** — gói riêng tư (manifest ẩn danh trả **403**) nên trượt hết rồi báo sai nguyên nhân *"chưa qua CI"* · `promotion-guard` **coi `skipped` là hỏng** (chặn đúng loại đề bạt hay gặp nhất) và **bỏ sót `Frontend — lint` hỏng** vì chỉ lọc tên bắt đầu bằng "Backend" · **commit tài liệu không có image** nên deploy dừng dù backend không đổi → lùi theo cha thứ nhất lấy image gần nhất.
  - ⚠ **Bản sửa đầu của tôi tự nó là lỗi**: `for muc in $runs` với tên job có dấu cách → bash tách từ, cổng **luôn đỏ**. **Thử ở máy local không lộ ra vì zsh không tách từ mặc định** — runner chạy bash. Luật: script của workflow phải kiểm bằng `bash -c`, không phải bằng shell mặc định của máy.
  - **Hai điều chấp nhận có ý thức, đã ghi để không bị hiểu nhầm**: `skipTestScope=true` nên **phụ thuộc phạm vi test không được quét CVE** (nợ #55) · `make hooks` là cấu hình **cục bộ từng bản clone**, quên chạy thì hook im lặng không tồn tại (nợ #54).
- ✅ **Rà soát + hoàn thiện tài liệu trước Phase 1 (19/8).** Xoá 2 mục đã sai sự thật ở `implement.md` (§7.1 còn ghi *"repo chỉ có tài liệu, 0 file mã nguồn → greenfield"*; §7.5 thứ tự khởi động Phase 0 đã xong) — thay bằng bia mộ giải thích, không xoá lặng lẽ. Sửa `conventions.md` còn ghi `ci.yml` 3 job. **Thêm `docs/coding-guide.md`**: bộ tài liệu có đủ *luật* nhưng thiếu *đường đi* — nay có bảng kê Core cho sẵn (chữ ký đọc từ mã nguồn), công thức 8 bước migration→test, 5 bẫy đã trả giá.
  - ⚠⚠ **Phát hiện khi viết tài liệu, không phải khi chạy test: `core/spi/` RỖNG** (nợ #56). ArchUnit chỉ cho import `<module>.spi.*` + `core.common.*`, mà 6 dịch vụ dùng chung (`WorkflowEngine`, `NotificationService`, `AttachmentService`, `JobService`, `SettingService`, `OrgUnitService`) đều ở `core.application.*` → **dòng mã Phase 1 đầu tiên gọi `WorkflowEngine` làm CI đỏ**. Phase 0 không lộ vì 4 module nghiệp vụ còn rỗng, chưa import gì từ core. **Chốt: mở SPI, giữ nguyên luật** (`architecture-review.md` §9.14) — ⛔ không nới ArchUnit cho `core.application.*`.
  - **Nguyên tắc rút ra**: *một ranh giới chưa ai đi qua thì chưa biết nó đúng hay sai*. ArchUnit xanh suốt Phase 0 chỉ chứng minh chưa ai thử — biến thể của §9.8.2 "xanh mà không chạy", lần này phép kiểm chạy đúng nhưng chạy qua **tập rỗng**.
- ✅ **Phase 1 đã có kế hoạch chi tiết (19/8)** — `phase1-tracking.md`: 11 hạng mục WS-12→WS-22, **101 task**, ~101 người-ngày, 17 mục Definition of Done. Quyết định kiến trúc + lý do ở `architecture-review.md` **§10** (9 mục).
  - **Ba thay đổi phạm vi so với `implement.md` §3**: **gộp hiển thị công khai vào Phase 1** (+8 pd — `POST /api/revalidate` dựng ở WS-9 "cho luồng duyệt bài Phase 1" mà **chưa ai đi qua**; để thêm một Phase nữa là tự nguyện nhận lại đúng rủi ro "xanh mà không chạy") · **giữ Liên hệ/Phản hồi ở Phase 2** (cùng pattern, chờ reCAPTCHA key) · **thêm đường nhập Excel có chạy khô** (+3 pd, cũng là đường seed khi G8 về).
  - ⚠⚠ **Ba cơ chế dựng ở Phase 0 tới giờ chưa ai đi qua**, và cả ba nằm trong DoD Phase 1 chứ không phải "kiểm sau": **ranh giới module** (ArchUnit chạy qua *tập rỗng* vì 4 module nghiệp vụ còn rỗng) · **phân quyền tầng 3** (chỉ chạy trên `ScopedRecord` dựng riêng cho test; app thật in *"Chưa có entity nào thuộc phạm vi đơn vị"*) · **ISR revalidate**.
- ✅ **WS-12 xong (19/8) — `core/spi/` đã mở, trả nợ #56.** 6 port + 7 record + 2 enum; `WorkflowAware` chuyển sang `core.common.persistence` (entity nghiệp vụ phải implement nó, mà `core.domain.*` là vùng cấm). Thêm **workflow nhiều trạng thái khởi đầu** (trạng thái-giả `__NEW__` ở vế `from_state`, 2 ràng buộc CHECK ở CSDL) và **hạn mức đính kèm theo chủ sở hữu** (`SYS-0010`). **284 test BE xanh** (211 core + 73 app) + 24 FE.
  - ⚠ **Lỗi im lặng có từ WS-6, lộ ra khi seed hạn mức**: `AttachmentService` đọc khoá `limit.upload.max-file-mb` **chưa từng được seed** → mọi lượt tải rơi về **20MB cứng trong mã**, trong khi 3 tham số `limits.upload.max-mb.*` bày ra ở giao diện mà **không dòng mã nào đọc**. Triệu chứng: quản trị viên đặt "tối đa 50MB", tải hồ sơ hoàn công 30MB, vẫn bị từ chối, không lời giải thích. Bài kiểm mới hỏi **"đổi tham số thì hành vi có đổi theo không"** — câu hỏi "mã có đọc được một con số không" sẽ xanh trọn vẹn suốt thời gian lỗi tồn tại.
  - ⏸ **T12.7 (ảnh phái sinh) hoãn có chủ đích → nợ #62**, lý do ở `architecture-review.md` **§10.9**. Đẩy sang `next/image` **không dùng được ở hệ này**: nó đòi `sharp` (native, nên vẫn là thêm thư viện, chỉ đổi chỗ đứng) · presigned URL TTL 10' làm **mỗi lượt gọi là một khoá đệm khác** và làm **vỡ ảnh trên trang ISR sống vài giờ** · `admin-app` là Vite nên **ảnh công trình không đi qua Next**, đẩy qua `/_next/image` còn là đi vòng qua phân quyền tầng 3. Phần đắt duy nhất là bộ mã hoá **WebP** (JDK không có); thumbnail thì rẻ vì `ImageSanitizer` đã chạy `ImageIO` sẵn từ WS-6. **⛔ Cố ý không seed tham số `settings`** cho công tắc này — công tắc chưa ai đọc chính là lỗi vừa sửa ở trên.
  - ⚠ **Việc này lớn hơn "thêm sáu interface"**: chữ ký cũ trả **entity domain** (`upload()` → `core.domain.attachment.Attachment`) — một interface đặt đúng chỗ nhưng *trả về* entity domain thì nơi gọi vẫn phải import nó, tức **SPI chỉ dời chỗ vi phạm chứ không xoá**. `ModuleBoundarySelfCheckTest` có bài riêng cho đúng dạng tinh vi này.
  - ⚠ **Kiểm chứng ngược trên mã production, không chỉ fixture**: đặt một lớp thật ở `content.application` nhận `SettingService` → luật **đỏ**, chỉ đích danh cả 3 cạnh phụ thuộc; đổi sang `SettingPort` → **xanh**.
  - ⚠⚠ **Bài canh mới bắt lỗi của chính tôi ở lượt chạy đầu**: `NotifySeverity`/`NotifyChannel` là bản sao có chủ đích của enum domain (để hợp đồng SPI không bị trói vào mô hình lưu trữ), và tôi **chép thiếu `DANGER` + `WEB_PUSH`**. Ánh xạ dùng `valueOf(name())` nên lỗi này **biên dịch trót lọt, test đơn vị vẫn xanh, rồi ném lỗi lúc chạy** đúng lúc có người bấm gửi thông báo. `NotificationEnumParityTest` kéo thời điểm phát hiện về CI. **Luật: chỗ nào con người phải nhớ hai nơi thì chỗ đó cần một phép kiểm nhớ hộ.**
  - 📋 **18 điểm nghiệp vụ đã làm rõ trước khi code** — spec không nói hoặc nói ra hai nghĩa: sửa bài đã xuất bản **không hạ bài xuống cổng** (copy-on-write) · bản ghi sửa chữa nhập sau khi xong bắt đầu thẳng ở "Đã xử lý" → **workflow phải nhận nhiều trạng thái khởi đầu**, ⛔ cấm lách bằng transition giả (hash chain đang ký tên vào lịch sử bịa) · **mọi cột tiền lưu VND** (spec ghi lẫn VND và triệu VND) · SVG chỉ nhận ở màn hình cấu hình và phải khử trùng · **không FK xuyên module** (`alert_event_public_id` không `REFERENCES`) · `articles` **không** thuộc phạm vi đơn vị.
  - ⭐ **Mở 3 mục mới cần Công ty**: **G13** bộ nhận diện cổng (logo/màu/GA/GTM/reCAPTCHA) · **G14** cây danh mục + menu + nội dung 4 trang tĩnh · **G15** "cụm công trình" là đơn vị tổ chức hay chỉ là cách nhóm. G13/G14 **không chặn code, chặn nghiệm thu** — cổng không logo không danh mục thì không có gì để Công ty xem. G15 đang đi vòng bằng phương án tối giản (**chưa dựng `construction_clusters`**), theo nguyên tắc *khi chưa biết thì chọn hướng mà cái sai rẻ hơn*.
- ✅ **WS-13 làm 10/12 (19/8) — dòng mã nghiệp vụ đầu tiên của dự án.** 4 bảng `cms` + seed quy trình `ARTICLE` (6 trạng thái, 10 bước chuyển) · entity/service/controller đủ 3 tầng · **55 mã lỗi** (BE=FE) · **298 test BE** (211 core + 87 app) + 24 FE. Còn T13.7 (job hẹn giờ) và T13.10 (đếm lượt xem) → nợ #63, #64, cả hai nhận ở WS-16.
  - ⭐ **Ba cơ chế Phase 0 lần đầu có người đi qua**: **ranh giới module** (ArchUnit nay chạy trên mã thật, không còn tập rỗng) · **workflow engine trên entity nghiệp vụ** · **tách vai trò bằng dữ liệu** — biên tập viên gọi `APPROVE` nhận `AUTH-3001` nhờ `workflow_transitions.required_permission`, không nhờ câu `if` nào.
  - ⚠⚠ **Người dùng đầu tiên lộ ra một lỗ ở Core**: `WorkflowEngine` chỉ biết luật G11 (Ban điều hành ∪ người phụ trách đơn vị) nên thông báo *"có bài chờ duyệt"* gửi cho **ban lãnh đạo Công ty** thay vì Quản trị nội dung — tức là cơ chế `notify_event` của WS-6 **chưa từng dùng được cho một quy trình duyệt nào**. Thêm `notify_permission` + `notify_owner` vào `workflow_transitions` (`architecture-review.md` **§10.10**). ⚠ Có `notify_permission` thì Ban điều hành bị **thay thế**, không cộng dồn — spam mọi lượt gửi duyệt là cách chắc chắn nhất để vài tuần sau **không ai đọc thông báo nữa, và cảnh báo sự cố thật chết theo**.
  - ⭐ **Hai cột quyết định hai việc khác nhau** ở `articles`: `published_version_id` quyết định cổng hiển thị **nội dung nào**, `status` quyết định **có hiển thị hay không**. Gộp làm một thì hoặc bài biến mất khỏi cổng lúc biên tập viên bấm Lưu, hoặc bản chưa ai duyệt lên thẳng trang chủ. Đây là chỗ copy-on-write sống.
  - ⚠ **`unaccent()` một tham số không phải IMMUTABLE** nên không vào được biểu thức chỉ mục; và **HQL không đoán được kiểu trả về của hàm tự định nghĩa** → `Operand of 'like' is of type 'java.lang.Object'`. Phải khai qua `FunctionContributor` nạp bằng `ServiceLoader`. Hàm `sn_khong_dau` đặt ở **`core`**, số hiệu migration nhỏ hơn mọi migration dùng nó — **Flyway sắp thứ tự theo số hiệu trên toàn bộ các thư mục**, nên `cms` bắt đầu từ 1016 là có chủ ý (`architecture-review.md` §10.11).
  - ⚠ **Đổi tên tệp migration để lại bản cũ trong `target/classes`** — Maven copy tài nguyên theo kiểu tăng dần, không xoá tệp đã biến mất. Flyway thấy hai migration cùng số hiệu và chết lúc khởi động. Bắt buộc `mvn clean` sau khi đổi tên migration.
  - ⚠ **Bài toán con-gà-quả-trứng của materialized path**: `path` chứa chính id do CSDL sinh, mà cột là `NOT NULL` → phải đặt path tạm, flush lấy id, rồi đặt path thật. `OrgUnitService` đã giải từ WS-6 và tôi vẫn đi lại vào đúng bẫy đó.
- ✅ **WS-14 xong (19/8) — thư viện media, và đóng DoD #11 của Phase 0.** `media_folders` (cây 3 cấp) + tệp là `attachments` với `owner_type='MEDIA_FOLDER'` — **không bảng tệp thứ hai**. **319 test BE** (220 core + 99 app) + 24 FE.
  - ⭐⭐ **MinIO THẬT trong test tích hợp (`SongnhueMinio`) — phát hiện nặng nhất của WS-14.** Từ WS-6 tới hết Phase 0, `app.storage.endpoint` trỏ vào `http://minio.invalid:9000`. `MinioClient` không mở kết nối lúc dựng bean nên context vẫn lên, 300 bài kiểm vẫn xanh, và **chưa một lượt tải tệp nào đi tới kho lưu trữ** — toàn bộ pattern P3 chỉ được kiểm ở phần *trước* khi chạm ra ngoài. Nay mọi test tích hợp chạy trên MinIO thật, cùng image với `compose.infra.yml`. Dùng `GenericContainer` chứ không thêm module `org.testcontainers:minio` — bớt một phụ thuộc phải theo dõi CVE.
  - ⚠ **Suýt kết luận nhầm là lỗi production**: lượt tải đầu tiên tới kho thật xong thì `downloadUrl` trả `SYS-0009` "chưa quét virus". Mã **không hỏng** — `VirusScanHandler` đánh dấu `SKIPPED` + `READY` khi chưa cấu hình ClamAV, đúng thiết kế WS-6; chỉ là bước đó chạy bằng worker, mà worker tắt ở môi trường kiểm thử. ⛔ **Cố ý không bật worker nền cho bài kiểm** — đưa một vòng lặp bất đồng bộ vào giữa phép khẳng định là tự nhận loại đỏ-thỉnh-thoảng; gọi thẳng bộ xử lý cho cùng đường đi mà tất định.
  - ⭐ **`SvgSanitizer` + 9 bài kiểm** — SVG là thứ duy nhất trong nhóm ảnh **chạy được JavaScript**, mà `FileValidator` không giúp được (SVG là văn bản thuần, **không có magic bytes**) và `ImageSanitizer` cũng không (`javax.imageio` không đọc SVG nên trả nguyên bản). Không có lớp này thì SVG đi thẳng từ biểu mẫu vào kho, **không qua lớp kiểm nào**. Mỗi bài khẳng định **cả hai vế**: đoạn nguy hiểm mất và hình vẽ còn — chỉ kiểm vế đầu thì một hàm trả chuỗi rỗng cũng xanh.
  - Thêm chữ ký **MP4/WebM** vào `FileValidator` (MP4 mang `ftyp` ở **byte 4**, đó là lý do `Signature` có trường offset) + nhóm dung lượng `video` 500MB.
- ✅ **WS-15 xong (19/8) — cấu hình giao diện, menu, banner.** 2 bảng (`banners`, `menu_items`) + 22 tham số nhóm `SITE` + seed khung danh mục/menu/4 trang tĩnh (G14). **62 mã lỗi** (BE=FE), **340 test BE** (220 core + 120 app) + 24 FE. Quyết định ở `architecture-review.md` **§10.12–§10.15**.
  - ⚠⚠ **Phát hiện nặng nhất: `SvgSanitizer` dựng ở WS-14 CHƯA BAO GIỜ ĐƯỢC GỌI.** `FileValidator.detect()` trả `null` cho mọi tệp SVG — SVG là XML thuần, **không có magic bytes** — nên `detectAndValidate` từ chối chúng ở *mọi* đường tải lên, kể cả đường mà chốt của dự án cho phép. Lớp khử trùng có 9 bài kiểm riêng, xanh trọn vẹn, và không nằm trên đường chạy nào. Lại đúng dạng lỗi đã trả giá nhiều lần. Sửa: `detect()` **đoán** SVG (bỏ BOM → cắt khoảng trắng → bắt buộc mở đầu bằng `<` → tìm `<svg`), và nhánh khử trùng đặt **ở tầng đính kèm chứ không ở nơi gọi** — chọn định dạng nào là việc của `allowedMimeTypes`, còn khử trùng thì không nơi gọi nào được phép quên. Bài kiểm đọc lại **từ MinIO** để chứng minh.
  - ⭐ **Cấu hình cổng ở `settings` nhóm `SITE`, không có bảng `site_config`** — ranh giới: *nhiều dòng người dùng thêm/bớt → bảng; một giá trị cho cả hệ thống → `settings`*. Nhưng dữ liệu chung **không có nghĩa là quyền chung**: mở `SettingAdminPort` với **mọi hàm mang `groupCode`**. Port ghi tự do `update(key, value)` thì chốt chặn duy nhất là annotation trên controller — *một dòng người ta có thể quên*; buộc khai nhóm biến giới hạn thành thứ máy kiểm được.
  - ⚠ **Bộ nhớ đệm cấu hình dọn bằng sự kiện, không bằng sự tự giác** — cùng một dòng `settings` sửa được từ **hai** màn hình (cấu hình giao diện của CMS và cấu hình hệ thống của MOD-05). Tự dọn trong hàm của mình thì đường kia không dọn gì: Quản trị viên hệ thống đổi tên cổng, giao diện báo thành công, cổng vẫn hiện tên cũ tới hết TTL. `SettingService.update` phát `SettingChangedEvent`; nghe bằng **`@TransactionalEventListener(AFTER_COMMIT)`** — dọn *trước* commit thì lượt đọc kế tiếp nạp lại giá trị cũ rồi giao dịch rollback, và không còn ai dọn lần nữa.
  - ⭐ **Ràng buộc "mục con cùng vị trí với cha" thi hành ở CSDL** bằng `UNIQUE (id, position)` + khoá ngoại ghép `(parent_id, position)`. CHECK không diễn đạt được (chỉ thấy một dòng); chặn ở service thôi thì một lượt seed hoặc sửa tay vẫn dựng được cây sai, và cổng hiển thị sai **mà không có lỗi nào**.
  - ⛔ **Cố ý KHÔNG seed tham số widget thuỷ văn** dù T15.5 ghi "giữ chỗ cấu hình" — widget cần MOD-03 (Phase 2) nên chưa dòng mã nào đọc được khoá đó, và công tắc chưa ai đọc chính là lỗi vừa sửa ở WS-12. Cũng **không** thêm `site.maintenance-mode`: `system.maintenance-mode` đã có từ WS-7 và đang được đọc thật. Có bài kiểm canh cả hai.
  - ⚠⚠ **Hai lỗi của chính bộ kiểm thử, lộ ra khi có dữ liệu seed.** (1) `DELETE FROM categories` của các bài kiểm cũ vi phạm khoá ngoại từ `menu_items`. (2) Mốc "dòng do bài kiểm tạo" đặt theo `created_by IS NOT NULL` **không chạy**: `AuditorAwareImpl` đọc `AuditContext` (do *filter* đặt) chứ không đọc `AuthContext`, mà test tích hợp chỉ đặt cái thứ hai — nên **mọi dòng đều `created_by = NULL`**, phép dọn không xoá gì, dữ liệu tích tụ qua từng bài. Gom về `CmsFixtures`, phân biệt bằng **mốc id**. Hệ quả rộng hơn đã ghi thành nợ **#66**: cột `created_by`/`updated_by` **chưa từng được kiểm chứng**.
  - **Seed khung danh mục/menu/trang tĩnh — không mâu thuẫn với việc V…1008 cố ý không seed cơ cấu tổ chức**: `org_units` là dữ liệu **chịu tải** (phân quyền, hồ sơ công trình neo vào id) nên đoán sai là phải di chuyển dữ liệu; danh mục và menu là dữ liệu **trình bày**, sửa mất vài phút. Cái giá của việc để trống thì ngược lại: cổng rỗng thì không có gì để Công ty xem lúc nghiệm thu. Bốn trang tĩnh đặt thẳng ở **Xuất bản** (nếu để Nháp thì menu trỏ vào 404) — **không** có bước chuyển giả nào ghi vào lịch sử, `created_by` để `NULL` nên nhật ký không nói rằng có người duyệt.
- ✅ **WS-16 xong (20/8) — cổng công khai chạy thật, và WS-13 đóng nốt (nợ #63, #64).** 7 lớp `content` + 8 trang Next · `HtmlSanitizer` (jsoup, danh sách **cho phép**) · nhóm hạn mức `PUBLIC` 300/phút · tệp công khai phục vụ **qua BE**, không presigned (URL 10' thì trang ISR sống vài giờ sẽ vỡ ảnh). **384 test BE** (232 core + 152 app) + **47 FE**. Đo trên `make dev-docker`: trang chủ **0,265 s** (NFR-02 cho 3 s) · revalidate → cổng đổi nội dung sau **114 ms** · sitemap **10** url · bộ đếm lượt xem lên đúng 7/7. Quyết định ở `architecture-review.md` **§10.16–§10.21**.
  - ⭐ **`POST /api/revalidate` dựng ở WS-9 nay đã có người đi qua** — một trong ba cơ chế Phase 0 chưa ai chạm tới.
  - ⚠⚠ **`revalidateTag` KHÔNG chữa được tuyến đường dựng hỏng lúc build.** Ảnh cổng build ở CI khi chưa có backend, nên trang chủ ra đời rỗng — và lượt `fetch` hỏng thì **không có mục cache nào mang nhãn**, nên nhãn không có gì để lần ngược tới. Đo thật: gửi nhãn hai lần, cả hai trả `{"revalidated":true}`, trang **vẫn rỗng**; gửi `{"path":"/"}` → có nội dung ngay. Phải hâm nóng **theo đường dẫn** sau khi khởi động, gồm cả `/sitemap.xml` (trước: **1** url; sau: **10**).
  - ⚠⚠ **Cổng dựng ra trang trắng trong Docker**: `NEXT_PUBLIC_API_BASE_URL` là địa chỉ của *trình duyệt* và bị nướng vào bundle lúc build; lượt gọi phía máy chủ nằm trong container nên phải có `API_INTERNAL_BASE_URL` riêng.
  - ⚠ **`HttpClient` của JDK mặc định HTTP/2** và gửi kèm yêu cầu nâng cấp h2c trên HTTP thuần; máy chủ Node của Next đóng kết nối → `header parser received no bytes`. `curl` (HTTP/1.1) chạy được nên suýt truy sai hướng. Ép `.version(HTTP_1_1)`.
  - ⚠⚠ **`CsrfFilter` chặn `/api/v1/public/**`** → `POST …/views` trả 403, tức bộ đếm lượt xem **không bao giờ chạy được** ở production. CSRF là tấn công **mượn phiên**; khách vãng lai không có phiên nào để mượn. Bài kiểm giữ **cả hai vế**: đường công khai không 403, đường quản trị **vẫn** 403.
- ⚠⚠ **Luật ArchUnit thứ ba, sau khi cùng một lỗi sập lần thứ hai: cấm tự gọi hàm `@Transactional`.** `ViewCountService.dayXuongDinhKy()` tự gọi `day()` bằng `this` → không qua proxy → ném `TransactionRequiredException` mỗi phút trong log bộ hẹn giờ, **chưa từng ghi được một lượt xem nào**; bài kiểm xanh vì nó gọi thẳng `day()` — **đi một đường khác với production**. (Lần một: `BackupService`, WS-7.) Luật chạy lần đầu tìm ra **8 vi phạm trong mã production**:
  - ⚠⚠ **`NotificationService.notify(NotifyRequest)` — cửa vào SPI của mọi module nghiệp vụ — đang chạy trong giao dịch `readOnly`.** Khối SPI thêm ở WS-12 chèn vào **giữa** một `@Transactional(readOnly = true)` và hàm nó thuộc về (`inbox()`), nên **chú thích rơi nhầm sang `notify`** — chú thích Java bám vào *khai báo kế tiếp*, không bám vào đoạn chú giải, và ở đây khoảng cách là tám dòng. Kiểm chứng ngược: PostgreSQL từ chối thẳng `cannot execute INSERT in a read-only transaction`. Lỗi sống qua **4 WS và hơn 370 bài kiểm** vì chưa ai đi cửa SPI (`WorkflowEngine` ở trong `core` nên gọi thẳng bản kia).
  - ⚠ **`CodeGenerator`**: nạp chồng tiện dụng gọi bản `REQUIRES_NEW` bằng `this` → bộ đếm mã **rơi vào chính giao dịch nghiệp vụ mà nó phải đứng ngoài**; lượt ghi hỏng thì bộ đếm lùi theo và bản ghi kế tiếp mang **lại đúng mã đó** — đúng thứ cả lớp sinh ra để chống.
  - 3 chỗ còn lại là chú thích ghi một bảo đảm không tồn tại (`BackupService.pruneExpired`, `JobService.findActiveByDedupKey`, `SettingService.getString`).
  - ⭐ **Bản sửa được kiểm chứng ngay, không hoãn**: `NotificationPortTest` tiêm **interface** `NotificationPort` — tiêm lớp cài đặt là gọi đúng hàm production *không* gọi. Đây cũng là **bài kiểm tích hợp đầu tiên** của cơ chế thông báo dựng từ WS-6.
  - ⚠ `./mvnw -pl app test` **không dựng lại `core`** → lượt kiểm chứng đầu đo nhầm jar cũ trong `~/.m2`. Phải có `-am`; CI dựng cả reactor nên không dính.
- ✅ **WS-20 xong (20/8) — màn hình CMS, kéo lên trước WS-17 theo quyết định của anh Quân.** 13 tệp `features/cms` + 5 component soạn thảo · danh sách/biểu mẫu/duyệt/diff/cây danh mục/thư viện media/cấu hình giao diện. **391 test BE** (239 core + 152 app) + **112 FE** (79 admin + 33 public). Quyết định ở `architecture-review.md` **§10.22–§10.26**.
  - ⚠⚠ **Đổi trình soạn thảo sang TipTap (MIT)**: tới 8/2026 **cả CKEditor 5 lẫn TinyMCE đều đã là GPL**, dùng thì phải phát hành mã nguồn admin-app theo GPL khi bàn giao — quyết định pháp lý của chủ đầu tư, không phải của người viết mã.
  - ⚠⚠ **`EditorVocabularyTest` — bài kiểm liên ngôn ngữ (Java đọc `editorSchema.ts`) — bắt 4 lỗi im lặng ở lượt chạy đầu**: `<s>` bị `HtmlSanitizer` gỡ (jsoup `relaxed()` chỉ có `strike`, thẻ HTML5 đã loại bỏ) · **nhúng video YouTube/Vimeo bị gỡ sạch** dù CN-01.1 yêu cầu · căn lề phải đi bằng `class` vì `style` bị cấm · và **chính bài kiểm gộp hai nguyên nhân làm một**, lượt đỏ đầu chỉ đường sai. Nguyên tắc: *thẻ nào trình soạn thảo sinh ra thì bộ lọc phải cho qua* — không thì người dùng soạn được, lưu thành công, rồi mất nội dung mà không có thông báo nào.
  - ⚠ **`Array.from` KHÔNG gộp dấu tổ hợp** — bộ đếm ký tự SEO đếm `Đề` dán từ Word ra **4** thay vì 2, và người soạn sẽ cắt bớt một tiêu đề hợp lệ. Phải `Intl.Segmenter`.
  - ⚠ **`useEffect` đổ dữ liệu vào biểu mẫu** bị ESLint chặn, và lý do sâu hơn tên luật: màn hình vẽ một lượt với ô trống rồi vẽ lại, người dùng kịp gõ vào khoảng giữa thì cú gõ đó bị ghi đè. Tách hai lớp + `initialValues` + `key`.
- ✅ **Rà soát đường chèn ảnh (20/8) — 4 lỗi im lặng, lỗi nặng nhất KHÔNG nằm ở trình soạn thảo.** Chèn tại con trỏ vốn đã chạy (`insertContent`); nay thêm **kéo-thả** (chèn tại *chỗ thả* qua `posAtCoords`, không phải chỗ con trỏ cũ) · **dán ảnh chụp màn hình** · **ô giữ chỗ lạc quan** (`blob:` hiện ngay, thay `src` thật khi tải xong) · chú thích + `alt` + bề ngang + căn lề trên thanh công cụ theo ngữ cảnh. **112 test FE** (79 admin + 33 public). Quyết định ở `architecture-review.md` **§10.25–§10.26**.
  - ⚠⚠ **Cổng công khai chưa từng có CSS cho nội dung bài** — thân bài mang class `prose` mà **`@tailwindcss/typography` chưa bao giờ được cài**, nên đó là class rỗng. Cộng với preflight của Tailwind xoá hình dạng mặc định: danh sách mất dấu đầu dòng, `h3`/`h4` bằng cỡ chữ đoạn văn, bảng không viền, chú thích ảnh không phân biệt được với một câu trong bài, `sn-align-*` không định nghĩa ở đâu. **Màn hình xem trước trong admin-app vẫn đúng** (nó dùng CSS của trình soạn thảo) nên biên tập viên không có cách nào biết. Tài liệu của `AlignClass` đã ghi sẵn điều kiện *"với điều kiện cổng công khai có định nghĩa ba class đó"* — **điều kiện được viết ra và không ai thực hiện**.
  - ⚠⚠ **Căn lề ảnh chưa bao giờ hoạt động** — `AlignClass` khai áp dụng cho `'image'`/`'figure'`, mà nút ảnh đăng ký tên `figureImage`. Đo bằng `getSchema`: nút ảnh **không hề có thuộc tính `align`** (TipTap bỏ qua lặng lẽ type không tồn tại). Lệnh vẫn trả `true` vì `NHOM_AP_DUNG.some(...)` **dừng ở phần tử đầu** và `'paragraph'` luôn trả `true` — nút sáng lên như đã xong việc, ảnh đứng yên.
  - ⚠ **Chú thích ảnh không có đường nào tạo ra được**: `caption` khai trong node, `figcaption` trong safelist, có trong mẫu kiểm, bài kiểm xanh — mà `RichTextEditor` truyền cứng `caption: null`, không ô nhập nào. CN-01.1 đòi *"căn lề, caption"*: hai vế, cả hai hỏng.
  - ⚠ **Kéo tệp ảnh vào bài làm mất bài đang soạn** — không chặn `drop` thì trình duyệt **điều hướng cả tab sang tệp vừa thả**.
  - ⚠⚠ **Bài canh cho lỗi CSS ban đầu XANH trong khi không kiểm gì**: nó hỏi `CSS.includes('.sn-align-center')`, mà chuỗi đó còn nằm ở một quy tắc khác cùng tệp — xoá hẳn `text-align: center` vẫn xanh. Nay hỏi **thuộc tính có được khai không**; kiểm chứng ngược bắt đủ 3 lượt phá hoại. ⛔ **Luật: canh cấu trúc, đừng canh văn bản.**
  - ⛔ **Giữ HTML, không lưu `jsonb`** (§10.25): nỗi lo "parse ra DOM bị lỗi" đã **đo thật** — jsoup 1.23.1 giữ nguyên thẻ inline sát chữ, câu dài có inline ở giữa, và thụt lề trong khối mã; nó chỉ thêm thụt lề *giữa các thẻ khối*, và bài kiểm khứ hồi chứng minh việc đó không đổi cây nút. Đổi sang `jsonb` thì **mất `HtmlSanitizer`**, cổng hết dựng được HTML nếu không mang schema TipTap sang máy chủ, và vỡ 3 thứ đang chạy (so sánh phiên bản · tìm kiếm toàn văn · chế độ soạn HTML).
  - ⛔ **Không presign đường TẢI LÊN** — bỏ qua `FileValidator` (magic bytes) · `ImageSanitizer` (**EXIF mang toạ độ GPS** của công trình thuỷ lợi) · `SvgSanitizer` · ClamAV · hạn mức `settings`, mà ta không có bộ bắt sự kiện MinIO để quét bù.
- ⚠⚠ **Trình duyệt CHƯA TỪNG gọi được API — CORS chặn toàn bộ giao diện quản trị (20/8).** Lộ ra khi anh Quân mở màn hình đăng nhập thật. Backend **cố ý không cấu hình CORS** (production có nginx đứng trước, cùng origin), nhưng compose lại build bundle trỏ `http://localhost:18080` trong khi giao diện ở cổng 15173 → khác origin → preflight trả **`403 Invalid CORS request`** → **không một lượt gọi nào chạy được**, suốt từ WS-8 tới WS-20. Quyết định ở `architecture-review.md` **§10.29**.
  - ⚠⚠ **Vì sao mọi lượt kiểm trước đều xanh**: chúng gọi bằng `curl` thẳng vào cổng backend. WS-20 *"4 route CMS trả 200, API trả 401"* và WS-16 *"bộ đếm lượt xem lên đúng 7/7"* đều đúng — nhưng chỉ chứng minh **backend** chạy, không chứng minh **trình duyệt** gọi được. `curl` không có origin, không có chính sách cùng nguồn, **không làm preflight**, nên đi lọt qua đúng bức tường chặn người dùng thật. ⛔ **Luật: endpoint mà trình duyệt phải gọi thì lượt kiểm phải mang `Origin`.**
  - **Chốt: trình duyệt luôn gọi CÙNG origin**, tầng phục vụ lo chuyển tiếp — nginx của image admin-app (`location /api/`), Route Handler ở public-web, `server.proxy` của Vite khi chạy native, nginx chung ở production. Không sinh ra đường đi nào mà production không có.
  - ⚠⚠ **`rewrites()` của Next bị nướng vào lúc BUILD** với `output: 'standalone'` — container có đúng `API_INTERNAL_BASE_URL` mà log vẫn `ECONNREFUSED 127.0.0.1:8080`. Phải dùng Route Handler (chạy mỗi request) để một image dùng được cho mọi môi trường.
  - ⚠⚠ **`proxy_pass http://app:8080` làm nginx phân giải DNS lúc nạp cấu hình** → backend chưa lên là `[emerg] host not found in upstream` và container quay vòng; **sự cố backend kéo theo cả trang trắng**. Sửa bằng `resolver` + biến trong `proxy_pass`; đo lại `RestartCount 0`. ⚠ Và `resolver ${NGINX_LOCAL_RESOLVERS}` chỉ có giá trị khi bật `NGINX_ENTRYPOINT_LOCAL_RESOLVERS` — script của image thoát sớm nếu thiếu.
  - ⚠ **Chuỗi rỗng không phải nullish**: compose truyền biến để trống thì Vite/Next nhúng vào bundle `''`, mà `??` giữ nguyên nó → `baseURL = ''` → mất hẳn tiền tố `/api/v1`. Phải dùng `||`.
- ⚠⚠ **Biến môi trường "để trống" KHÔNG hề trống (20/8)** — `architecture-review.md` **§10.27**. Docker Compose không cắt chú thích khi giá trị rỗng: `BOOTSTRAP_ADMIN_PASSWORD=    # [T] chỉ dùng 1 lần` vào container thành `"# [T] chỉ dùng 1 lần"`. **37 dòng** dính, cả ba tệp `local/staging/prod.env.example`.
  - ⚠⚠ Hậu quả nặng nhất: `AdminBootstrapRunner` chạy ở **mọi** lượt khởi động và đặt mật khẩu `superadmin` thành **chính đoạn chú thích** — một chuỗi nằm trong tệp `.example` **đã commit lên repo**. Các biến khác: `DB_RESTORE_PASSWORD` (bật nhầm chức năng phá huỷ nhất trong hệ), `SMTP_*` (thử `AUTH` với chuỗi rác → hỏng gửi thư), `GOOGLE_MAPS_API_KEY`.
  - `UnresolvedPlaceholderGuard` (T4.8) không bắt được vì giá trị **giải ra bình thường**, chỉ là giải ra sai, và không rỗng nên `@NotBlank` cũng qua. Lần trước là *"thiếu biến mà tưởng có"*, lần này *"có biến mà tưởng thiếu"*. Lỗi **chỉ ở đường Docker** — chạy native thì shell cắt chú thích đúng, nên hai lối chạy cho hai hành vi khác nhau từ cùng một tệp.
- ✅ **Font Noto Sans tự host thay CDN Google (20/8)** — `architecture-review.md` **§10.28**. `docs/ui-styles.md` bản đầu quy định `@import url('https://fonts.googleapis.com/…')`, và admin-app còn nạp thêm một lượt bằng `<link>` (cùng bộ chữ **tải hai lần**). ⛔ `conventions.md` §4.5 đã chốt CSP `default-src 'self'` → WS-11 dựng nginx là font **bị chặn**, trang rơi về font hệ thống **không lỗi nào**. Thêm nữa: cổng của doanh nghiệp nhà nước gửi IP của **mọi người dân tra cứu** sang Google mỗi lượt tải trang — cùng lý do đã chọn `youtube-nocookie`. Đo sau khi đổi: **48 tệp `woff2`** trong bản dựng mỗi app, **0** tham chiếu ra Google.
  - ⭐ **Bộ từ vựng chuyển sang `design-tokens/src/editor-schema.ts`** — nó là hợp đồng của **ba** bên: soạn thảo (admin-app sinh thẻ) · khử trùng (backend giữ) · **hiển thị (public-web dựng CSS)**. Cạnh thứ ba chưa từng có phép canh nào; nay mỗi cạnh một bài kiểm.
- ➡️ **Thứ tự mới đã chốt (20/8)**: WS-20 ✅ → **WS-17** (danh mục công trình) → **WS-23** (nền biểu đồ + dashboard điều hành, hạng mục **mới thêm** vào Phase 1 để có màn hình demo; wall mode 4K nhưng **phải co giãn** xuống laptop/monitor rời) → WS-18, WS-19, WS-21 → WS-22. ⛔ **Cấm seed dữ liệu công trình/thuỷ văn "cho đẹp demo"** — ô nào chưa có nguồn thì nói thẳng là chưa có.
- ➡️ **Còn lại**: **WS-17** (danh mục công trình — nơi phân quyền tầng 3 lần đầu chạy trên entity thật, **và** nơi module nghiệp vụ đầu tiên đi qua cửa SPI thông báo); cân nhắc kéo **WS-20** (màn hình CMS) lên trước WS-17→19 để đóng nợ #65 lúc API CMS còn ổn định và Công ty có màn hình nghiệm thu G13/G14 — **chờ anh quyết**; gửi **8 mục còn mở** (G3-a, G5, G6, G8, G9-a, G10, G13, G14) + `report-templates-proposal.md` cho Công ty; **WS-11 (Deploy — 10 pd)** vẫn treo để khép Phase 0.
  - ✅ **Nợ #35 (vòng đời mật khẩu M5.15-a) chốt sang Phase 2 (19/8, ~4 pd)** — hạn 90 ngày chỉ cắn sau go-live 90 ngày nên không nằm trong kịch bản nghiệm thu Phase 1; quên mật khẩu đã có đường quản trị viên cấp mật khẩu tạm. ⛔ Không suy ra rằng chính sách mật khẩu đang tắt: BCrypt cost 12, độ dài tối thiểu, bắt đổi lần đầu, khoá sau 5 lần sai đều chạy từ WS-5 — phần hoãn chỉ là hết hạn 90 ngày + nhắc trước hạn + luồng tự phục vụ. WS-11 đang giữ nhiều nợ nhất: #26 (VM + compose staging/prod), #29 (VM-3 + Prometheus/Grafana), #30 (Alertmanager), #31 (nginx chặn actuator/swagger). Hai việc bấm ở GitHub còn treo: **#45 bật Dependency graph** (không bật thì `Soi phụ thuộc PR thêm vào` tự bỏ qua — phép kiểm chưa chạy lần nào) và **#27 chỉnh 2 mục bảo vệ nhánh**.

## Quy ước làm việc với user

- User: QuanTran (quantran@goapps.team). Trả lời tiếng Việt, ngắn gọn, đi thẳng vào vấn đề.
- Khi cập nhật quyết định kiến trúc: sửa `architecture-review.md` trước, rồi đồng bộ sang `function-spec.md` và `implement.md`.
